import { MentionDirective } from '.';

describe('MentionDirective', () => {
  it('should create an instance', () => {
    const directive = new MentionDirective();
    expect(directive).toBeTruthy();
  });
});
